---
title: App metadata still says "Create Next App"
severity: low
category: quality
file: src/app/layout.tsx
---

## Problem
`src/app/layout.tsx` exports:

```ts
export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};
```

These show up in browser tabs, social cards, and search results.

## Why it matters
Looks unfinished. Anyone bookmarking the app or sharing a link advertises "Create Next App" instead of the product.

## Status
**Resolved alongside the landing page work** in this same effort — the new root layout updates `title` to `"Weavy — Visual AI Workflow Builder"` and adds a real description and Open Graph metadata.

## Acceptance criteria
- [x] Title says "Weavy — Visual AI Workflow Builder".
- [x] Description is product-relevant.
- [ ] (Future) Add `openGraph` and `twitter` cards with a hero screenshot.
